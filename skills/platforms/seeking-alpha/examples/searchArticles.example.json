{
  "operation_id": "searchArticles",
  "replay_safety": "safe_read",
  "cases": [{
    "input": { "filter[query]": "AAPL", "filter[type]": "articles", "page[size]": 3 },
    "assertions": { "status": 200, "response_schema_valid": true }
  }]
}
