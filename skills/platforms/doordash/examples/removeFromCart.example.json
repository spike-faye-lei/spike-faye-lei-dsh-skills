{
  "operation_id": "removeFromCart",
  "method": "post",
  "replay_safety": "unsafe_mutation",
  "order": 2,
  "cases": [
    {
      "input": {
        "cartId": "${prev.addToCart.addCartItemV2.id}",
        "itemId": "${prev.addToCart.addCartItemV2.orders.0.orderItems.0.id}"
      },
      "assertions": {
        "status": 200,
        "response_schema_valid": true
      }
    }
  ]
}
